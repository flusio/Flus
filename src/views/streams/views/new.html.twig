{% extends 'layouts/modal.html.twig' %}

{% block title %}{{ t('New view') }}{% endblock %}

{% block body %}
    {{ include('streams/views/_form.html.twig', {
        form: form,
        action: url('create stream view', { id: stream.id }),
        submit_label: t('Create the view'),
        url_parameters: url_parameters,
    }) }}
{% endblock %}
