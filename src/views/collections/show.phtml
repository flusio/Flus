<?php
    $this->layout('base.phtml', [
        'title' => $this->protect($collection->name),
        'canonical' => url('collection', ['id' => $collection->id]),
        'current_tab' => 'collections',
    ]);
?>

<div class="section">
    <nav class="section__nav">
        <a class="anchor--action icon icon--anchor icon--undo" href="<?= url('collections') ?>">
            <?= _('Collections') ?>
        </a>

        <div class="section__nav-separator"></div>

        <a
            href="<?= url('edit collection', ['id' => $collection->id]) ?>"
            title="<?= _('Settings') ?>"
            aria-label="<?= _('Settings') ?>"
        >
            <i class="icon icon--anchor icon--only icon--cog"></i>
        </a>
    </nav>

    <div class="section__title section__title--nolowercase">
        <h1><?= $this->protect($collection->name) ?></h1>
    </div>

    <p class="section__intro">
        <?= $this->protect($collection->description) ?>
    </p>

    <p class="paragraph--centered">
        <a
            class="anchor--action icon icon--anchor icon--plus-circle"
            href="<?= url('new link', ['collection' => $collection->id]) ?>"
        >
            <?= _('Add a link') ?>
        </a>
    </p>

    <div class="links">
        <?php foreach ($links as $link): ?>
            <div class="link">
                <div class="link__body">
                    <h2 class="link__title">
                        <?= $this->protect($link->title) ?>
                    </h2>

                    <p class="link__details">
                        <?= $this->protect($link->host()) ?>&nbsp;·&nbsp;<?= format_reading_time($link->reading_time) ?>
                    </p>
                </div>

                <div class="link__footer">
                    <form method="post" action="<?= url('remove link collection', ['id' => $link->id]) ?>">
                        <input type="hidden" name="csrf" value="<?= csrf_token() ?>" />
                        <input type="hidden" name="collection_id" value="<?= $collection->id ?>" />
                        <input type="hidden" name="from" value="<?= url('collection', ['id' => $collection->id]) ?>" />

                        <button
                            class="button--discreet"
                            title="<?= _f('Remove from %s', $collection->name) ?>"
                            aria-label="<?= _f('Remove from %s', $collection->name) ?>"
                        >
                            <i class="icon icon--only icon--solid icon--ghost icon--collection"></i>
                        </button>
                    </form>

                    <a class="anchor--action" href="<?= url('link', ['id' => $link->id]) ?>">
                        <?= _('see') ?>
                    </a>
                </div>
            </div>
        <?php endforeach; ?>
    </div>

    <?php if (!$links): ?>
        <p class="paragraph--placeholder">
            <?= _('This collection is empty. The links will appear here once you start adding them.') ?>
        </p>
    <?php endif; ?>
</div>
