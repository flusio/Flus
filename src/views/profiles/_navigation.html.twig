<div class="stack stack--gap-small">
    <a
        class="button button--ghost"
        href="{{ url('profile', { id: user.id }) }}"
        {{ page === 'links' ? 'aria-current="page"' : '' }}
    >
        {{ t('Links') }}
    </a>

    <a
        class="button button--ghost"
        href="{{ url('profile collections', { id: user.id }) }}"
        {{ page === 'collections' ? 'aria-current="page"' : '' }}
    >
        {{ t('Collections') }}
    </a>

    <a
        class="button button--ghost"
        href="{{ url('profile streams', { id: user.id }) }}"
        {{ page === 'streams' ? 'aria-current="page"' : '' }}
    >
        {{ t('Streams') }}
    </a>
</div>
