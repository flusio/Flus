{% if message %}
    <div class="alert alert--info" role="alert" data-turbo-temporary>
        <div class="alert__title">{{ t('Info') }}</div>

        <p class="alert__message">
            {{ message }}
        </p>
    </div>
{% endif %}
