<?php
    $this->layout('base.phtml', [
        'title' => $link->title,
        'canonical' => url_full('show link', ['id' => $link->id]),
    ]);
?>

<div class="section">
    <div class="section__title">
        <h1><?= $this->protect($link->title) ?></h1>
    </div>

    <p class="link__details paragraph--centered">
        <?= $this->protect($link->host()) ?>&nbsp;·&nbsp;<?= format_reading_time($link->reading_time) ?>&nbsp;·&nbsp;<a href="<?= url('show update link', ['id' => $link->id]) ?>"><?= _('edit') ?></a>
    </p>

    <p class="paragraph--centered">
        <a
            class="anchor--action icon icon--anchor icon--link"
            target="_blank"
            rel="noopener noreferrer"
            href="<?= $link->url ?>"
        >
            <?= _('open') ?>
        </a>
    </p>
</div>
